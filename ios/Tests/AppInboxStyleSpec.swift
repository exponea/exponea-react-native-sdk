import Foundation
import Quick
import Nimble

@testable import Exponea

class AppInboxStyleSpec: QuickSpec {
    override func spec() {
        context("StyleParser") {
            it("should parse Color correctly") {
                let conf = AppInboxStyle()
            }
        }
    }
}
